apiVersion: apps/v1
kind: Deployment
metadata:
  name: api-gateway
  namespace: pulsecart
spec:
  replicas: 2
  selector:
    matchLabels:
      app: api-gateway
  template:
    metadata:
      labels:
        app: api-gateway
    spec:
      containers:
        - name: api-gateway
          image: 971146591534.dkr.ecr.us-east-1.amazonaws.com/triad-app-api-gateway:b412b873845965ff94741558b910c4dd5bbbf0bf6344f536bb663938f3125908
          imagePullPolicy: Always
          ports:
            - containerPort: 8080
              name: http
          env:
            - name: PORT
              value: "8080"
            - name: ORDERS_URL
              valueFrom:
                configMapKeyRef:
                  name: pulsecart-config
                  key: ORDERS_URL
          readinessProbe:
            httpGet:
              path: /readyz
              port: http
          livenessProbe:
            httpGet:
              path: /healthz
              port: http
